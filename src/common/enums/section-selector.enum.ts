export enum SectionSelector {
  PRICES = "#prices",
  FEATURES = "#features",
  SERVICES = "#services",
  MASTERS = "#masters",
  GALLERY = "#gallery",
  ABOUT_US = "#about",
  CONTACTS = "#contacts",
}
