export enum FormFieldType {
  TEXT = "text",
  DATE = "date",
  NUMBER = "number",
  SELECT = "select",
  ACCOUNT_SELECT = "accountSelect",
  CATEGORY_SELECT = "categorySelect",
  TEXTAREA = "textarea",
  COLOR = "color",
}
