export enum FormFieldType {
  TEXT = "text",
  DATE = "date",
  NUMBER = "number",
  SELECT = "select",
  BOOLEAN = "boolean",
  ACCOUNT_SELECT = "accountSelect",
  CATEGORY_SELECT = "categorySelect",
  TEXTAREA = "textarea",
  COLOR = "color",
  IMAGE = "image",
}
